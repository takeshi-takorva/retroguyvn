name: Materialize static assets

on:
  push:
    branches:
      - refactor/static-assets-v1
  workflow_dispatch:

permissions:
  contents: write

jobs:
  materialize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/static-assets-v1
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: node scripts/materialize-assets.mjs
      - name: Commit generated static assets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/images/brand/retroguy-wordmark.webp public/images/dr-portal/products/*.webp
          if git diff --cached --quiet; then
            echo "No generated asset changes"
            exit 0
          fi
          git commit -m "chore: materialize static website images"
          git push origin HEAD:refactor/static-assets-v1
