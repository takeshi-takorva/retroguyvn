{
  "name": "retroguyvn-web",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "prepare:assets": "node scripts/materialize-assets.mjs",
    "dev": "npm run prepare:assets && astro dev",
    "build": "npm run prepare:assets && astro build",
    "preview": "astro preview",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.2.9"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
